已完成
- 写完了Aria流水线需要的结构，无锁链表（其实是小锁链表）和4个Sid
- 还差修改调用work_enqueue和work_dequeue的地方
- 还差对Sid的管理
todo：


存在的问题：
1. 快照问题。
2. 乱序水印问题。

3. 时间戳清理机制问题。已有解决方案：
- Aria验证时候的两个时间戳，得改成lr和lw，未提交的(未过水印的)读/写事务列表  ->  这个用来对应清理时间戳的机制。

----------------------------------------
1.21日
1. 今天调Aria的流水线通了，但是性能烂的一批。
2. 所以想试试原版的aria，目前原版aria也有问题，跑不出来，刚好是每一个batch差最后一个事务收不回来，明天再调吧！ ✅1.22日

----------------------------------------
1.22日
1. Aria流水线性能差，是因为 水印 的实现有问题。

----------------------------------------
1.26日
1. Aria的水印有想法了，改成把水印的指针，存在msg里，这样调整REMOVED，直接打一个标记就行，然后由统一的线程去进行清理(这个定期清理的时间得改改，现在改成一毫秒了，不知道效率怎么样)

2. 和高斯开会，高斯表示负载会周期发生变化，比如白天是业务，晚上是另一套业务

----------------------------------------
2.3日
1. Calvin的分布式测出来有问题
2. 单机Aria的水印搞定了
----------------------------------------
3.18日
1. Calvin的分布式还没搞定，不过稍微搞定另一个bug。
2. Calvin的分布式问题，目前看起来是，T2（排在后面的）事务不知道为啥在T1前跑了。。。没拿完锁就tm开始跑了，太奇怪了
----------------------------------------
3.19日
1. Calvin的分布式问题搞定了
2. Aria的pipline分布式性能贼烂，得看看
----------------------------------------
3.24日
1. 首先，Aria得整一个全局水印
2. 其次，Aria目前的并发控制方法有问题
3. 最后，即使我不考虑并发控制，现在分布式也贼烂，目前猜测可能在队列上？现在的表现是，只要接收远程消息，性能就不正常，即使不需要远程返回
    目前已经将2PC注释掉了，目前的远程操作只有远程读写操作；流程是类似于T1需要在N2上有远程读，就发一个读消息给N2，在N2给返回以前，不能进入写阶段；当N2给了返回，T1确认远程读结束，才发送第二波远程写给N2。
    刚才尝试了，如果注释掉 远程返回的检查，以及远程消息的处理，性能回复正常，两台机器加起来6w
    注释掉远程返回的检查，但是处理远程消息，两台机器加起来3w
----------------------------------------
3.25日
1. 把系统的代码整体清理了一下
----------------------------------------
3.26日
1. SDOCC（即pip Aria）重新了一部分
    1. 已完成 数据项的处理、事务到执行阶段的流程、定序器
    2. 未完成 水印机制接入、验证失败重试的逻辑
----------------------------------------
3.31日
1. SDOCC写完
----------------------------------------
4.1日
1. SDOCC性能不对劲
    1. 单机Calvin性能2w8, Silo 10w, ARIA 8w

    2. 单机正常（所有机制全打开）性能1w出头
    3. 单机check强制成功（不重试）性能2w5 (validation)
    4. check强制成功+不更新水印，性能15。。。。。。这个就很奇怪 
    5. check强制成功+不更新水印+不把事务塞到水印里, 性能1w3??????


    6. 以上+完全越过Sequencer，9w-7w，tmd；不过7w也不对劲啊？怎么这么低？
    7a. 以上+关掉从small_lock_list队列中取事务，性能7w，tmd
    7b. 以上+关掉并发控制，性能8-11w不等，tmd
    8. 以上+关掉从small_lock_list队列中取事务+关掉并发控制，性能11w
----------------------------------------
4.2
确认了，现在写的定序器（calvin的定序器）有问题。Silo加上以后，10w->4500😂
----------------------------------------
4.3
上午把Silo的定序器换成Aria的，性能7-9w不定，idle_time是0；
然后又花了一下午时间，找出来idle_time不正常的问题，在于之前删除No_Remote的时候删错了😅，已经修复了

但是Silo+Aria定序器对不对，还得再看
----------------------------------------
4.7
现在Silo+定序器的性能不对，idle_time=0，而且性能影响很大
----------------------------------------
4.8
今天开始一点点排查，定序器的问题。
1. 已经测试只是在定序器转发不会影响性能(定序器拿到事务以后，直接转发给worker)
2. 如果是将事务塞到定序器的batch队列里，也影响不大11w->10w，所以把ACK直接交给worker，问题应该出在seq又要给事务打标记，又要统计事务的ACK
3. 目前删掉了Seq的ACK，直接由Worker返回，性能10w，可以用。
4. 目前好像是跑通了，定序器、watermark、重试都打开，性能10w。
----------------------------------------
4.10
今天跑了下分布式，看起来性能还不错。
----------------------------------------
4.13
可以开始跑测试了
发现问题，原本pipcalvin(SDPCC)高冲突下性能也会炸😅
----------------------------------------
4.14
现在有几个问题。
1. SDOCC低冲突下不如Calvin，只是比Aria强；可能可以调整一下；
    1. 目前的思路就是改成。。Aria和Calvin模式，也就是一次性远程读的，应该能好点
2. SDPCC高冲突性能雪崩，是因为锁队列维护开销太大+事务队列维护开销太大，需要遍历超1w没有用的事务。
3. TPCC还没跑

循环队列
----------------------------------------
4.21
SDPCC高冲突搞定了
----------------------------------------
4.22-4.23
SDOCC低冲突下其实也不如Aria。。。。。。🤦🏻‍♀️
加了一次性远程读的，现在性能太高了，高冲突1.5下也8w，有可能消息通信还是哪里出问题了。
需要调试

所以目前差的
1. SDOCC + 已知读写集性能调整
2. TPCC
----------------------------------------
4.24
SDOCC性能差的原因找到了，重试次数太多，导致一个事务发了一大堆prepare消息。。。。。。果然不断重试是不合理的。
这个改改应该就行了
----------------------------------------
4.29
SDOCC改成了只有水印到了才重试，目前SDOCC改成了未知读写集的版式。
现在SDOCC高冲突下性能很好。。。。。